# Language setting
language: java

# Version number
jdk:
  - openjdk7
  - openjdk8
  - openjdk9
  - oraclejdk8
  - oraclejdk9
  
build:
   

    
  ci:
    - java -version  
    - echo $SHIPPABLE_JDK_VERSION
    - sudo docker images
    - mvn --version
    - gradle --version 
    - ant -version  
    


