# Language setting
language: java

# Version number
jdk:
  - openjdk7
  - openjdk8
  - oraclejdk7
  - oraclejdk8

build:
  ci:
    - java -version
    #- ant -version
    - gradle --version
    - mvn --version
 
  pre_ci_boot:
    image_name: drydock/u16javall
    image_tag: master
    pull: true 

