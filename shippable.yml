# Language setting
language: java

# Version number
jdk:
  - openjdk7
  - openjdk8
  - oraclejdk7
  - oraclejdk8

build:
   
  pre_ci_boot:
    image_name: drydock/u16javall
    image_tag: master
    pull: true 
    
  ci:
    - java -version
    - gradle --version
    - mvn --version 
    - ant -version
